class YatzyEngine {

    calculateScore (category, diceValues){
        //returns the score for the given category based on the dice values passed as an array
    }

    isValidSelection (category, diceValues){
        //checks whether the current dice are valid for the category the player is trying to score in
    }

    updateScore (player){
        //this method updates the current player's score
    }
}